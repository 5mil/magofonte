# MagoFonte environment variables
# Copy to .env and fill in values — never commit .env

# Stratum wallet address for upstream authorization
STRATUM_USER=your.wallet.address

# Supabase (used by vault module)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# HTTP API port (default 8080)
PORT=8080
